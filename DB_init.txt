CREATE TABLE FILM(
IDFILM INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
TITOLO VARCHAR(100),
TRAMA VARCHAR(100),
TRAILER VARCHAR(30),
GENERE VARCHAR(30));

CREATE TABLE CLIENTEREGISTRATO(
IDCLIENTE INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
NOME VARCHAR(30),
PASSWORD VARCHAR(30),
EMAIL VARCHAR(30),
CARTADICREDITO VARCHAR(30));

CREATE TABLE SALA(
IDSALA INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
CAPIENZA INT);

CREATE TABLE PROIEZIONE(
IDPROIEZIONE INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
DATA DATE,
ORARIO TIME,
IDFILM INT,
IDSALA INT,
FOREIGN KEY (IDFILM) REFERENCES FILM(IDFILM),
FOREIGN KEY (IDSALA) REFERENCES SALA(IDSALA));

CREATE TABLE BIGLIETTO(
IDBIGLIETTO INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
COSTO FLOAT,
NUMPOSTO INT,
IDCLIENTE INT,
IDPROIEZIONE INT,
FOREIGN KEY (IDCLIENTE) REFERENCES CLIENTEREGISTRATO(IDCLIENTE),
FOREIGN KEY (IDPROIEZIONE) REFERENCES PROIEZIONE(IDPROIEZIONE));

INSERT INTO CLIENTEREGISTRATO VALUES (0, 'ClienteNonReg','','','');
--------------------------------------------------------------------------------------------------

INSERT INTO FILM VALUES (NULL, 'Il Gladiatore', 'Mi chiamo Massimo Decimo Meridio', 'link', 'storico-drammatico');
INSERT INTO SALA VALUES (NULL, 200);
INSERT INTO PROIEZIONE VALUES (NULL, '2020-01-01', '16:20', 1, 1);
INSERT INTO CLIENTEREGISTRATO VALUES (NULL, 'Mario', 'pippo1', 'mariorossi@gmail.com', '1111222233334444');
INSERT INTO BIGLIETTO VALUES (NULL, 8, 101, 1, 1), (NULL, 8, 102, 1, 1);






